# Core camera + AI
pytapo>=2.0
ultralytics>=8.0
pyyaml>=6.0
Pillow>=10.0
opencv-python-headless>=4.8
numpy>=1.24

# License plate OCR (lazy-loaded, optional)
easyocr>=1.7

# External intelligence feeds
paho-mqtt>=1.6  # Blitzortung MQTT broker support

# Audio event classification (YAMNet + Silero-VAD)
# Adds ~2.6 GB to the container image. Only loaded when audio_events: true
# is set on a camera in cameras.yaml.
tensorflow>=2.13
tensorflow-hub>=0.14
torch>=2.0
torchaudio>=2.0
librosa>=0.10
scipy>=1.10
